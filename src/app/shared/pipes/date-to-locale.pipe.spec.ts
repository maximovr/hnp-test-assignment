import { DateToLocalePipe } from './date-to-locale.pipe';

describe('DateToLocalePipe', () => {
  it('create an instance', () => {
    const pipe = new DateToLocalePipe();
    expect(pipe).toBeTruthy();
  });
});
